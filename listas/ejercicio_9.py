from biblioteca import *

lista_enteros = cargar_lista_enteros("Ingrese un numero entero", 0)

mostrar_lista(lista_enteros)

print(f"La cantidad de numeros enteros de la lista es: {len(lista_enteros)}")